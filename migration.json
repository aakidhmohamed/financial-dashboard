{
    "query": "ALTER TABLE invoices ADD COLUMN IF NOT EXISTS payment_account_id UUID REFERENCES accounts(id); ALTER TABLE invoices ADD COLUMN IF NOT EXISTS advance_paid NUMERIC DEFAULT 0; CREATE INDEX IF NOT EXISTS idx_invoices_payment_account_id ON invoices(payment_account_id);"
}